bcrypt==4.3.0
blinker==1.9.0
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.2
colorama==0.4.6
Flask==3.1.3
Flask-JWT-Extended==4.7.1
Flask-SQLAlchemy==3.1.1
greenlet==3.5.3
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
PyMySQL==1.2.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
requests==2.34.2
SQLAlchemy==2.0.51
stripe==15.3.0
typing_extensions==4.16.0
urllib3==2.7.0
Werkzeug==3.1.8

# --- Driver PostgreSQL (SOLO si usas Supabase) ---
# Los tests usan SQLite y NO requieren este paquete.
# Si tu Python es <= 3.12 puedes hacer: pip install psycopg2-binary
# Si tienes Python 3.13/3.14 (sin wheels aún), instala PostgreSQL local
# (https://www.postgresql.org/download/windows/) para tener pg_config, o
# ejecuta: pip install psycopg2-binary --no-binary :all:

# --- Dependencias de testing ---
pytest==8.4.2
pytest-flask==1.3.0
pytest-mock==3.14.1
pytest-cov==7.0.0

# Crea tu entorno virtual con python -m venv venv y luego activa con venv\Scripts\activate
# Instala todas las dependencias con pip install -r requirements.txt
# Corre los tests con pytest -v (desde la raíz del proyecto)